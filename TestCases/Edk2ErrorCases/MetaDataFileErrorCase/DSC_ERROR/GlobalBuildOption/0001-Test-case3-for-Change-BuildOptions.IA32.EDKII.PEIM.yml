---
name: Test case3-E for Change BuildOptions.IA32.EDKII.PEIM
description: Change build option under BuildOptions.IA32.EDKII.PEIM (Add "XCODE:*_*_*_DLINK_FLAGS = -seg1addr 0x1000 -segalign 0x1000") in TestPkg/buildoptions.dsc.

command: build -p TestPkg\TestPkg32X64.dsc -a X64 -a IA32 -t VS2015x86 -b RELEASE -y report.txt
input:
  type: repo
  source: edk2

change:
  - 0001-Test-case3-for-Change-BuildOptions.IA32.EDKII.PEIM.patch

output:
  - edk2\Build
    