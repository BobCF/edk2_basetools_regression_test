---
name: Test case2-E for Change build option under BuildOptions.IA32
description: Change build option under BuildOptions.IA32 (Add "-D CRB_FLAG") in TestPkg/buildoptions.dsc.

command: build -p TestPkg\TestPkg32X64.dsc -a X64 -a IA32 -t VS2015x86 -b RELEASE -y report.txt
input:
  type: repo
  source: edk2

change:
  - 0001-Test-case2-for-Change-build-option-under-BuildOption.patch

output:
  - edk2\Build
    